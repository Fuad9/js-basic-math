// If the variable remains unchanged we will use const instead of var
// If the variable has the chance to be changed later, we will use let instead of var

let age = 12;
age = 14;
console.log(age)
const name = "Abul";
console.log(name);